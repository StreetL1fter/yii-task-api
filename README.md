# Task API (Yii2)

REST API для управления задачами.  
Реализованы все CRUD-операции через `TaskController` с вынесенной бизнес-логикой в `TaskService`.

## Endpoints

- `POST /tasks` — создать задачу  
- `PUT /tasks/{id}` — обновить задачу  
- `DELETE /tasks/{id}` — удалить задачу  
- `GET /tasks/{id}` — получить задачу  

## Особенности
- Валидация входных данных
- Обработка ошибок (404, 422, 200, 201, 204)
- Сервисный слой для изоляции бизнес-логики

## 📌 Важно: как работать с API

Все запросы к API **требуют заголовок** `X-User-ID`, который указывает ID пользователя, от имени которого выполняется действие.

### 💡 Зачем это нужно?

Чтобы:
- Убедиться, что пользователь имеет право создавать/редактировать/удалять задачи,
- Защитить данные от несанкционированного доступа.

### 🔧 Пример использования (Postman)

1. Открой Postman.
2. Выбери метод (например, `POST`).
3. В поле URL введи: http://localhost:8080/tasks
4. Перейди во вкладку **Headers**.
5. Добавь строку:
Key: X-User-ID
Value: 1 или 2
6. Перейди во вкладку **Body** → выбери `raw` → `JSON`.
7. Вставь тело запроса:
```json
{
  "user_id": 1 или 2,
  "title": "Новая задача",
  "description": "Описание...",
  "status": "pending"
}
Нажми Send.
✅ Если всё правильно — ответ будет 201 Created.
❌ Если нет заголовка или ID неверный — 400 Bad Request или 403 Forbidden.

Проект запускается с помощью Open Panel Server. Все 4 файла которые есть,а это: 1. .osp  2. my-yii-app  3. .gitignore  4.README. Должны 
быть вместе
